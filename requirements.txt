# Core dependency (required)
pandas>=2.0.0

# Optional: only needed if you use --llm with OPENAI_API_KEY set.
# Uncomment to install:
# openai>=1.30.0

# Testing (optional, for running the test suite)
pytest>=7.0.0
